QUnit.module( "Document - basic mechanism" );

QUnit.test( "out of the box x10d document support", function( assert ) {

	logSummary(assert, "This is a newly created x10d object with support for relationships, but none added so far.");
	assert.ok( 1 == 1, "Not yet developed." );
});
